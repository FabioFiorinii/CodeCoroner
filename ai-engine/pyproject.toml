[project]
name = "codecoroner-ai-engine"
version = "0.1.0"
description = "CodeCoroner AI agents engine"
requires-python = ">=3.13"
dependencies = []

[tool.ruff]
target-version = "py313"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "W", "N", "UP", "B"]
